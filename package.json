{
  "name": "ai-pizza-quality-checker",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/data-tables": "^13.2.2",
    "@azure/functions": "^4.0.0",
    "@azure/openai": "^1.0.0-beta.11",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^18.x",
    "@types/uuid": "^9.0.8",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  },
  "main": "dist/src/functions/*.js"
}
